describe("Sample Test", () => {
  it("should test that true === true", () => {
    expect(true).toBe(true);
  });
});

describe("Math Test", () => {
  it("should test that 1+1 === 2", () => {
    expect(1 + 1).toBe(2);
  });
});
